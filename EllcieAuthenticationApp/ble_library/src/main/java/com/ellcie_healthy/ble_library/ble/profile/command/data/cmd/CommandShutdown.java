package com.ellcie_healthy.ble_library.ble.profile.command.data.cmd;

public class CommandShutdown extends CommandResponse {

    public CommandShutdown() {
        super(CommandCode.COMMAND_SHUTDOWN_DEVICE);
    }
}
